version: '3.8'

services:
  # PostgreSQL Database (Local - Comment out if using external/cloud database)
  # Uncomment this section if you want to use a local PostgreSQL container
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: bitterrock-db
  #   restart: unless-stopped
  #   env_file:
  #     - .docker.env
  #   environment:
  #     POSTGRES_USER: ${DB_USER:-postgres}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
  #     POSTGRES_DB: ${DB_NAME:-bitterrock_db}
  #   ports:
  #     - "${DB_PORT:-5433}:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #     - ./database/schemas/schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - bitterrock-network

  # Next.js Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bitterrock-app
    restart: unless-stopped
    env_file:
      - .docker.env
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - DB_HOST=asia-southeast1-001.proxy.kinsta.app
      - DB_PORT=30339
      - DB_NAME=bare-magenta-magpie
      - DB_USER=viper
      - DB_PASSWORD=jP6=zZ4+hJ2_aD1-uM9+
      - DB_SSL=false
      - NEXT_PUBLIC_APP_URL=http://localhost:3001
      - R2_ACCESS_KEY_ID=f950fd678c608767ef973a91421097cc
      - R2_SECRET_ACCESS_KEY=5f6a85702d09715210c607aa6655e706e92b993588312c872a3ac2351d045e95
      - R2_ENDPOINT=https://f6d1d15e6f0b37b4b8fcad3c41a7922d.r2.cloudflarestorage.com
      - R2_BUCKET_NAME=apple-gold-anteater-it815
      - R2_PUBLIC_BASE=https://apple-gold-anteater-it815.sevalla.storage
    networks:
      - bitterrock-network
    volumes:
      - ./public/uploads:/app/public/uploads

volumes:
  postgres_data:
    driver: local

networks:
  bitterrock-network:
    driver: bridge

